# Rust
/target
**/*.rs.bk
*.pdb

# Node
node_modules/
npm-debug.log*

# VS Code extension build artefacts
vscode-extension/dist/
vscode-extension/*.vsix

# mezz's own generated output. `.mezz/settings.json` is deliberately NOT
# ignored — it describes the repo and is meant to be committed (see
# .mezz.example/README.md); only what mezz writes is.
.mezz/data/

# Runtime data from `mezz` itself
data.json
data.details.json
data.base-details.json
data.index.json
diff.json
visualization.html
deps.dot

# Local test fixtures / language samples (not published)
/data/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp

# Examples
/data